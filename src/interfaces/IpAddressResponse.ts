export interface IpAddressResponse {
  ".id": string;
  "actual-interface": string;
  address: string;
  disabled: string;
  dynamic: string;
  interface: string;
  invalid: string;
  network: string;
  slave: string;
  vrf: string;
}
